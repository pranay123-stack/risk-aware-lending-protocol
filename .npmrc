# Supply-chain hardening: dependency lifecycle scripts (postinstall etc.) never run.
# Everything this repo needs ships as prebuilt JS or optional platform packages.
ignore-scripts=true
engine-strict=true
auto-install-peers=true
